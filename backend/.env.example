# ========================================
# BACKEND ENVIRONMENT VARIABLES
# ========================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git (it contains secrets!)

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
# Development: http://localhost:3000
# Production: https://your-app.vercel.app (NO trailing slash!)
FRONTEND_URL=http://localhost:3000

# Database Configuration
# MongoDB connection string
# Development (Local): mongodb://localhost:27017/waveguard
# Production (Atlas): mongodb+srv://username:password@cluster.mongodb.net/waveguard?retryWrites=true&w=majority
# 
# For MongoDB Atlas (Production):
# 1. Go to https://cloud.mongodb.com
# 2. Create free M0 cluster
# 3. Database Access -> Add User -> Save password
# 4. Network Access -> Add IP -> Allow 0.0.0.0/0
# 5. Database -> Connect -> Get connection string
# 6. Replace <password> with your actual password
MONGO_URI=mongodb://localhost:27017/waveguard

# Firebase Admin SDK Configuration
# How to get these values:
# 1. Go to Firebase Console (https://console.firebase.google.com)
# 2. Select your project
# 3. Project Settings -> Service Accounts
# 4. Click "Generate New Private Key" -> Download JSON file
# 5. Extract the values from the JSON file:
#    - project_id -> FIREBASE_PROJECT_ID
#    - client_email -> FIREBASE_CLIENT_EMAIL
#    - private_key -> FIREBASE_PRIVATE_KEY
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_CLIENT_EMAIL=your_service_account@your_project.iam.gserviceaccount.com
# IMPORTANT: Keep the quotes and newline characters (\n)
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# AI Model Configuration (Optional)
# Directory to cache downloaded AI models
AI_MODEL_CACHE_DIR=/tmp/ai-models

# Location Verification Configuration
# Enable/disable location verification for join/upload actions
LOCATION_VERIFICATION_ENABLED=true
# Maximum allowed distance from challenge location in kilometers
LOCATION_MAX_DISTANCE_KM=5
# Testing mode - bypasses all location checks when true
# Development: can be true for easier testing
# Production: MUST be false
TESTING_MODE=false
# Comma-separated list of emails that bypass location checks (for dev/testing)
TESTING_BYPASS_EMAILS=dev@waveguard.com,tester@waveguard.com

# ========================================
# PRODUCTION DEPLOYMENT NOTES
# ========================================
# When deploying to Railway/Render:
# 1. Set NODE_ENV=production
# 2. Use MongoDB Atlas connection string for MONGO_URI
# 3. Set FRONTEND_URL to your Vercel URL (https://your-app.vercel.app)
# 4. Add all environment variables in platform dashboard
# 5. For Railway: PORT can be 5000
# 6. For Render: PORT should be 10000
# 
# See HOSTING_INSTRUCTIONS.md for complete deployment guide
