# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Database Configuration
# MongoDB connection string
# Local: mongodb://localhost:27017/waveguard
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/waveguard
MONGO_URI=mongodb://localhost:27017/waveguard

# Firebase Admin SDK Configuration
# Get these from Firebase Console -> Project Settings -> Service Accounts
# Download the JSON key file and extract these values
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_CLIENT_EMAIL=your_service_account@your_project.iam.gserviceaccount.com
# Private key - keep the quotes and newlines (\n)
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# AI Model Configuration (Optional)
# Directory to cache downloaded AI models
AI_MODEL_CACHE_DIR=/tmp/ai-models
